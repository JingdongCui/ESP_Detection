# Current Task

## 当前目标

- 在 Qt 上位机的“系统维护”第 4 页增加本地云端大模型回复展示，并在未连接板子时展示完整模拟数据。

## 当前现状

- `HostController` 已在断连状态返回固定数据并将所有展示状态显示为设备在线、链路正常；连接后自动恢复真实链路数据，控制操作仍不向未连接设备发送命令。
- `ReservePage.qml` 已保留维护功能，并增加本地大模型回复卡片：加载转圈 2 秒、约 20 字/秒逐字显示、完成后停留 10 秒循环；离页停止动画。
- 上位机构建和 CTest 已通过；未改动固件。

## 下一步

- 本轮已完成；后续如接入真实云端大模型，另行定义请求、响应与异常回退协议。

## 阻塞点

- 无；本轮未连接实物板，实机链路切换待现场验证。
