# Current Task

## 当前目标

- 暂无进行中的任务。

## 当前现状

- 板端工程与上位机工程已发布到 `git@github.com:JingdongCui/ESP_Detection.git` 的 `main` 分支。
- 远端保留两个本地仓库的完整提交历史；仓库根目录为板端，上位机位于 `esp32_host_no_inference/`。

## 下一步

- 等待新任务。

## 阻塞点

- 无。
