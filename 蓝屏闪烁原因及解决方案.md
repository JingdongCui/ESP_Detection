# 蓝屏闪烁原因及解决方案

## 问题现象

ESP32-P4 + EK79007（1024×600）MIPI DSI 屏幕，每隔一段不定时会蓝屏闪烁一瞬间，然后瞬间恢复，系统仍正常运行。

---

## 根因：MIPI DSI DPI 控制器 PSRAM 带宽不足导致 FIFO underrun

DPI 显示控制器以固定像素时钟（52 MHz）连续从 PSRAM 读取帧缓冲数据。当 PSRAM 总线被其他主控（CPU 取指、其他 DMA）瞬时抢占，DPI 内部 FIFO 取不到数据（underrun），屏幕立即输出默认填充色（蓝色）；下一帧 DMA 重启后画面恢复。这与"闪蓝一瞬 → 瞬间恢复 → 系统正常"的现象完全吻合。

---

## 证据链

### 1. 官方驱动源码直接描述了该症状

`$IDF_PATH/components/esp_lcd/dsi/esp_lcd_panel_dpi.c:105-109`：

```c
if (intr_status & MIPI_DSI_BRG_LL_EVENT_UNDERRUN) {
    // when an underrun happens, the LCD display may already becomes blue
    // it's too late to recover the display, so we just print an error message
    // as a hint to the user that he should optimize the memory bandwidth (with AXI-ICM)
    ESP_DRAM_LOGE(TAG, "can't fetch data from external memory fast enough, underrun happens");
}
```

underrun 时屏幕**变蓝**；DMA 传输完成回调 `mipi_dsi_dma_trans_done_cb`（`:77-84`）会 **restart the DMA transfer, keep refreshing the LCD**，下一帧重启后画面恢复。

### 2. 实时硬件寄存器证实显示链路正常运行

通过 JTAG / OpenOCD 读取 `MIPI_DSI_BRIDGE`（基址 `0x500a0800`）：

| 寄存器 | 值 | 含义 |
| --- | --- | --- |
| `EN.DSI_EN` | `1` | DSI 桥接器正在运行 |
| `INT_ENA.UNDERRUN_INT_ENA` | `1` | underrun 中断已启用 |
| `INT_RAW.UNDERRUN_INT_RAW` | `0`（轮询 800 次） | 见下方说明 |
| `FIFO_FLOW_STATUS.RAW_BUF_DEPTH` | `768 ~ 1020` | FIFO 深度，瞬时窗口内健康 |
| `DPI_RSV_DPI_DATA` | `0x3fff` | 无有效数据时输出的保留填充色 |

`INT_RAW` 持续读到 `0` **不能排除 underrun**：ISR `mipi_dsi_bridge_isr_handler` 第一步就执行 `clear_interrupt_status`（`esp_lcd_panel_dpi.c:103`）清除锁存位，瞬态事件在两次采样间隙即被清掉，单纯轮询寄存器抓不到。

### 3. 带宽计算解释为何"不定时"偶发

- 当前配置：`bsp_lcd.c` 中 `LCD_BIT_PER_PIXEL = 24` + `LCD_COLOR_PIXEL_FORMAT_RGB888`，像素时钟 52 MHz。
- DPI active 期突发读取带宽 ≈ **156 MB/s**（1024×600×3 字节/像素，扣除消隐期的有效行内突发）。
- PSRAM 200 MHz hex 模式理论峰值约 400 MB/s，实际有效约 60–70%。
- **关键抢占源**：`sdkconfig` 中

  ```
  CONFIG_SPIRAM_XIP_FROM_PSRAM=y
  CONFIG_SPIRAM_FETCH_INSTRUCTIONS=y
  CONFIG_SPIRAM_RODATA=y
  ```

  即 CPU 指令和只读数据也从同一块 PSRAM 取。当周期性任务（`system_monitor` 采样、以太网收包、冷代码路径的 cache-miss 突发）恰好与 DPI 取帧冲突时，DPI FIFO 瞬时断流 → underrun → 闪蓝。这正是"不定时"的来源。

### 4. 排除项：摄像头 CSI 不是带宽竞争源

`main/system_init.c:61` 的 `vision_app_start()` 已被注释，摄像头视觉主循环没有运行，`vision_preview_update()` 不会被调用，CSI 通路无 DMA 活动，因此可排除摄像头作为带宽竞争源。

---

## 修复方向（按性价比排序）

| 方案 | 带宽降幅 | 代价 |
| --- | --- | --- |
| **A. RGB888 → RGB565**（主帧缓冲改 16 位色深） | 156 → 104 MB/s，**降约 1/3** | 色深从 1670 万色降到 6.5 万色；需改 `bsp_lcd.c` 的 `LCD_BIT_PER_PIXEL` 与 DPI `pixel_format` |
| **B. 关闭 PSRAM XIP**（指令 / rodata 放回 Flash） | 消除 CPU 取指对 PSRAM 的抢占 | 牺牲部分 CPU 性能（Flash 比 PSRAM 慢） |
| **C. 提高 DSI DMA 优先级 / 启用 AXI-ICM** | 官方推荐的根治方向 | 需调 DMA `num_outstanding_requests`、burst 配置，较复杂 |
| **D. 降低帧率 / 像素时钟** | 线性降低带宽 | 刷新变慢，可能可见 |

**推荐组合：A + B**，改动小、风险低，通常能彻底消除 underrun。

---

## 运行时验证方法（可选）

underrun 发生时官方驱动会打印一条 `ESP_DRAM_LOGE` 错误日志：

```
can't fetch data from external memory fast enough, underrun happens
```

该日志走主控制台（UART），不走本项目的 SEGGER RTT。若要让每次闪屏都留下明确日志，可将其重定向到 RTT，或确认 UART 日志已接出。这样可在改动前先复现确认根因，改动后验证是否消除。

---

## 关键文件索引

| 文件 | 作用 |
| --- | --- |
| `components/bsp/bsp_lcd.c` | LCD / MIPI DSI 初始化，色深与 DPI 配置（`LCD_BIT_PER_PIXEL`） |
| `components/bsp/bsp_lvgl_adapter_init.c` | LVGL 显示适配，帧缓冲与防撕裂模式 |
| `$IDF_PATH/components/esp_lcd/dsi/esp_lcd_panel_dpi.c` | DPI 驱动与 underrun ISR（根因证据） |
| `managed_components/espressif__esp_lcd_ek79007/include/esp_lcd_ek79007.h` | EK79007 面板 DPI 时序宏 |
| `sdkconfig` | PSRAM XIP / 取指 / rodata 配置 |
